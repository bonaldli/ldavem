.First <- function() {
  options(
    repos = c(CRAN = "http://cran.rstudio.com/"),
    browserNLdisabled = TRUE,
    deparse.max.lines = 2, 
    devtools.name = "Clint P. George", 
    devtools.cleandoc = T, 
    devtools.desc.author = "'Clint P. George <clintpg@ufl.edu> [aut, cre]'", 
    devtools.desc.license = "GPL-3", 
    devtools.desc = c("Description" = "LDA Variational EM Algorithm")
    )
}

if (interactive()) {
  suppressMessages(require(devtools))
}

